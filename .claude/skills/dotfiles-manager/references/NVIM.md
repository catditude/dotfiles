# Neovim Reference

See `~/.config/nvim/CLAUDE.md` for full documentation.

## Git Commands

```bash
cd ~/.config/nvim
git add <files> && git commit -m "message" && git push
```

## Fetch Latest Docs

Use context7 MCP: `/neovim/neovim`, `/folke/lazy.nvim`, etc.
